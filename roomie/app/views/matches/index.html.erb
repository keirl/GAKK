<div id="jumbo1" class="jumbotron">
<p id="notice"><%= notice %></p>

<h1>Matches</h1>
<br>
<table width="30%" class="new_table">
  <thead>
    <tr>
      <th>Username</th>
      <th>Percent_Match</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @matches.each do |match| %>
      <tr>
        <td><%= link_to  User.find(match.user_id_2).profile.user_name, "profile/#{User.find(match.user_id_2).profile.id}" %></td>
        <td><%= match.percent_match %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>